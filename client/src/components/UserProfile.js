// Page of currently logged in user.
// Gives option to log out
// Links to component to publish new recipe
// Shows list of recipes published by currently logged in user

import React from "react";

const UserProfile = () => {
  return (
    <> </>
  )
}

export default UserProfile;